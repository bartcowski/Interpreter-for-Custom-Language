struct Person {
    string name;
    int age;
}

void main() {
    List[Person] people;

    List[Person] people222 = people.Where(Person.age >= 18 & Person.age < 70);
    print("All people222: ", people222);
}